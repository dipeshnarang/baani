import { Toolbar, styled } from "@mui/material";

export const AppHeader = styled(Toolbar)({
  width: "100%",
  mx: "auto",
  display: "flex",
  justifyContent: "space-between",
  padding: '1.2rem 3rem !important'
});
